前情回顾

1. 二叉树的层次遍历
2. 算法基础
3. 排序 ： 冒泡  选择   插入   快速
4. 查找 ： 二分查找

(book:数据结构python实现, 算法导论)
(公众号： 算法爱好者，python程序员，python开发者)
(app: 掘金，知乎)

5. 什么是IO
6. 字节串和字符串之间的转换

******************************************************

文件读写流程

1. open 打开文件
2. 读写文件
3. close 关闭文件

读操作

read([size])
1. 如果不加参数表示读到文件末尾，如果文件过大时不建议这样做，会消耗大量内存，读取时间也会更长
2. 如果读取到文件结尾再读取会得到空字符串
3. 实际读取到的内容可能小于等于指定的size大小

readline([size])
1.每次读取一行内容遇到换行终止
2.如果指定size，则只读取size大小的内容

readlines([size])
1. 读取到整行内容作为列表返回，每行是列表中一个元素
2. 如果写size，size不足一行也会得到整行内容

write(string)
1. 如果想换行需要人为添加换行符


注意：
如果open打开文件的方式中有b，表示以二进制打开，此时读写操作得到或者写入的内容都是字节串


练习 ： 从终端输入一个文件名称（可以夹带路径），将该文件复制到当前目录下，并且重名为 xxxx .要求可以复制所有类型文件


缓冲区： 减少和磁盘的交互次数，提高磁盘读写效率

1. buffer = 1 表示行缓存则在写入数据时遇到\n自动刷新缓冲区
2. 采用系统默认缓存需要缓冲区满后才能自动写入磁盘

3. 无论什么缓冲，当程序结束或者文件被关闭时都会将缓冲区内容写入磁盘

4. 通过调用flush()可以人为刷新缓冲


文件偏移量

功能： 代表文件的当前读写操作位置，随读写操作移动

1. r，w方式打开文件此时偏移量在开头
2. 以a 方式打开此时偏移量在末尾

文件描述符：

系统为每个IO操作分配的整数，不会重复


网络编程



作业:1.  编写一个程序完成如下。

      1. 每隔1秒向文件test.txt中写入一行数据，格式如下
			   
				1.  2019-7-30  12:12:12
			  2.  2019-7-30  12:12:13
				3.  2019-7-30  12:12:19
				    
						.....
			
			2. 该程序无限循环，ctrl-c退出
			3. 当重启程序时，内容会继续向下写，序号能够接上之前的

    2. 网络概念熟悉


